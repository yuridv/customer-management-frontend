import { Components } from '../../utils/imports';

const Page = ({ style }) => {
  return (
    <div className={ style.container }>
      <h1>Page Private /</h1>
    </div>
  );
};

export default Page;